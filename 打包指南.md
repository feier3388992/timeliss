# Capacitor 打包 HTML 为 APK 完整指南

## 一、原理：网页是怎么变成 APK 的？

### 核心概念
Capacitor 本质上做了一个"壳"：把你的 HTML/CSS/JS 文件塞进一个 Android 原生 App 里，然后用 Android 系统自带的 **WebView**（类似内置浏览器）来渲染你的网页。

```
┌─────────────────────────────────┐
│        Android APK              │
│  ┌───────────────────────────┐  │
│  │   Native Shell (Java)     │  │
│  │   ┌─────────────────────┐ │  │
│  │   │     WebView         │ │  │
│  │   │  ┌───────────────┐  │ │  │
│  │   │  │  index.html   │  │ │  │
│  │   │  │  02.png       │  │ │  │
│  │   │  │  CSS/JS...    │  │ │  │
│  │   │  └───────────────┘  │ │  │
│  │   └─────────────────────┘ │  │
│  └───────────────────────────┘  │
└─────────────────────────────────┘
```

### 打包流程图解
```
你的网页文件
    │
    ▼
Capacitor 读取 webDir 目录
    │
    ▼
复制到 Android 项目的 assets/public/ 目录
    │
    ▼
Android Studio 编译整个项目（Java + 资源文件）
    │
    ▼
Gradle 打包成 .apk 文件
    │
    ▼
安装到手机 → WebView 加载 assets/public/index.html
```

---

## 二、环境准备

### 必须安装
1. **Node.js** - 运行 npm 命令
2. **Android Studio** - 提供 Android SDK 和编译工具

### 网络问题
国内访问 Google 服务器慢，需要配置镜像：
- Gradle 下载 → 用腾讯/阿里镜像
- Maven 依赖 → 用阿里云镜像

---

## 三、详细操作步骤

### 第 1 步：初始化 npm 项目
```bash
cd 你的项目目录
npm init -y
```
生成 `package.json`，记录项目配置。

### 第 2 步：安装 Capacitor
```bash
npm install @capacitor/core @capacitor/cli @capacitor/android --save-dev
```
- `@capitor/core` - 核心运行时
- `@capacitor/cli` - 命令行工具
- `@capacitor/android` - Android 平台支持

### 第 3 步：初始化 Capacitor
```bash
npx cap init "App名称" "包名(如com.example.myapp)" --web-dir www
```
生成 `capacitor.config.json`，告诉 Capacitor 网页文件在哪。

### 第 4 步：添加 Android 平台
```bash
npx cap add android
```
生成 `android/` 文件夹，这是一个完整的 Android Studio 项目。

### 第 5 步：同步网页文件
```bash
npx cap sync
```
把 `www/` 里的文件复制到 `android/app/src/main/assets/public/`。

### 第 6 步：用 Android Studio 打开项目
```bash
npx cap open android
```
或手动打开 `android/` 文件夹。

### 第 7 步：配置国内镜像（重要！）
编辑 `android/build.gradle`，在 `repositories` 里加：
```gradle
maven { url 'https://maven.aliyun.com/repository/central' }
maven { url 'https://maven.aliyun.com/repository/public' }
maven { url 'https://maven.aliyun.com/repository/google' }
```

### 第 8 步：生成 APK
在 Android Studio 中：
1. 点击右侧 `Gradle` 面板
2. 展开 `app` → `Tasks` → `build`
3. 双击 `assembleDebug`

或在顶部菜单：`Build` → `Build APK`

### 第 9 步：找到 APK
```
android/app/build/outputs/apk/debug/app-debug.apk
```

---

## 四、关键文件说明

| 文件 | 作用 |
|------|------|
| `capacitor.config.json` | 配置 App 名称、包名、网页目录 |
| `android/build.gradle` | Gradle 构建配置，配置镜像源 |
| `android/app/src/main/AndroidManifest.xml` | Android 应用配置（权限、图标等） |
| `android/app/src/main/assets/public/` | 你的网页文件最终存放位置 |

---

## 五、换电脑怎么办？

直接复制整个项目文件夹（包含 `android/` 目录），新电脑上：

1. 打开 Android Studio → 打开 `android/` 文件夹
2. 如果依赖下载慢，用项目里的 `build.gradle` 覆盖
3. 如果 Gradle 下载慢，手动放入 `.gradle/wrapper/dists/` 目录
4. 点击 `Sync Project with Gradle Files`
5. 运行 `assembleDebug` 生成 APK

---

## 六、常见问题

### Q: 为什么 APK 有 4MB 这么大？
A: 因为包含了 Android 原生壳 + WebView 运行时，不是纯网页大小。

### Q: 修改了 HTML 怎么更新？
A: 修改 `www/index.html` 后运行 `npx cap sync`，再重新 Build。

### Q: 能调用手机摄像头、定位等功能吗？
A: 可以，Capacitor 提供了原生 API 插件，需要额外安装。

### Q: 怎么生成正式版 APK？
A: 需要配置签名密钥，运行 `assembleRelease`。
